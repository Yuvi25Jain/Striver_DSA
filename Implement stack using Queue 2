#include<iostream>
#include<stack>
#include<queue>
using namespace std ;

/*stack operation : lifo : last in first out

push(3) = 3
push (2) = 3 2
push (1) = 3 2 1 
s.top() = 1
pop() 3 2
pop() = 3 
top() = 1

implement using two   queues (FIFO)

two queue : q1 and q2

step 1 : if q1 is not empty then copy its element to q2 from its front
step 2 : push the new element to q1
step 3 : copy all element from q2 to q1 until q2 is empty.

push (3) = q1 = 3  q2 = empty
copy q1 to q2 -> q1 = empty , q2 = 3
push (2) = q1 = 2 , q2 = 3
copy q2 to q1 -> q1 = 2 3 , q2 = empty
push (1) = q1 = 2 3 1 , q2 = empty
copy q1 to q2 -> q1 = empty , q2 = 2 3 1
copy q2 to q1 = 1 3 2 q2 empty.


*/

class st{
    queue<int> q1 ;
    queue<int> q2 ;
    mystack(){


    }
    public: 
    void Push(int x){
        while (!q1.empty()){
            q2.push(q1.front());
            q1.pop(); //copy the element of q1 to q2.
        }
        q1.push(x) ; // push the new element to empty q1. 

        //copy all element from q2 to q1
        while(!q2.empty()){
            q1.push(q2.front());
            q2.pop();
        }
        // q1 latest element at front and q1.size() - 1 is the last element OR previous element.
    }
        int Pop(){
            int ans = q1.front();
            q1.pop() ;
            return ans;
        }
        int Top(){
            return q1.front() ;
        }
        bool isempty(){
            return q1.empty() ; 
        }


    
};

int main() {
  st s;
  s.Push(3);
  s.Push(2);
  s.Push(4);
  s.Push(1);
  cout << "Top of the stack: " << s.Top() << endl;
//   cout << "Size of the stack before removing element: " << s.Size() << endl;
  cout << "The deleted element is: " << s.Pop() << endl;
  cout << "Top of the stack after removing element: " << s.Top() << endl;
//   cout << "Size of the stack after removing element: " << s.Size();
  return 0 ; 

}
